import React from 'react';
import { graphql, useStaticQuery } from "gatsby"
import Helmet from "react-helmet"

const Head = props => {
    return (
      <Helmet>
      </Helmet>
    )
}

export default Head;